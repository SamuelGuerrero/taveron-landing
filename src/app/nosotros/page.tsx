import { type Metadata } from "next";
import { UnderConstruction } from "~/components/templates";

export const metadata: Metadata = {
  title: "Nosotros — Taveron",
};

export default function NosotrosPage() {
  return <UnderConstruction />;
}
